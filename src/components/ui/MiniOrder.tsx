import { View, Text } from "react-native";
import React from "react";

type Props = {};

const MiniOrder = (props: Props) => {
  return (
    <View>
      <Text>MiniOrder</Text>
    </View>
  );
};

export default MiniOrder;
