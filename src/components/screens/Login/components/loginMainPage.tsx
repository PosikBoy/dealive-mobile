import { View, Text } from "react-native";
import React, { useEffect } from "react";
import * as Font from "expo-font";
import { useFonts } from "expo-font";
type Props = {};

const loginMainPage = (props: Props) => {
  return (
    <View>
      <Text>Dealive</Text>
    </View>
  );
};

// const styles =
export default loginMainPage;
